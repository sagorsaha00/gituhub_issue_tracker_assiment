    - 1️⃣ What is the difference between var, let, and const?
    ans: javascript var is ago JavaScript variable declaration keyword; var declar from block scope; it value change again
       let is es6 latest version its block scope and its value change
       const is a block scope and do not change value or do not decalr again

    - 2️⃣ What is the spread operator (...)?
    ans:the spread operator is used to expand elements of an array or object. It helps to copy arrays, or pass values easily and plus ohter value array
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    ans: map() is used to create a new array by transforming each element of an array.
        filter() is used to create a new array with elements that match a condition.
        forEach() is used to loop through an array, but it does not return a new array.
 - 4️⃣ What is an arrow function?
       ans: arrow function is a shorter way to write functions in JavaScript. It was introduced in ES6 and uses the => syntax. - 5️⃣ What are template literals?
       ans:template literals are a way to write strings in JavaScript using backticks ( ). They allow us to include variables and expressions inside a string easily use $this and {} ;
